<h1>Book a Flight</h1>
<%= form_with url: "/search", method: :get do |form| %>
  <%= form.label :query, "Departure Airports:" %>
  <%= select_tag(:departure_airport_id, options_for_select(@departure_airport_options))%>
  <%= form.submit "Search" %>
<% end %>